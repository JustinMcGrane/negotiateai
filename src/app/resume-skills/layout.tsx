import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Skills for a Resume — Best Resume Skills List & Examples | Hayven',
  description: 'Discover the best skills to put on a resume. Browse top technical skills, soft skills, and computer skills with examples for every job type.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
